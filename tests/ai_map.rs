use bevy::prelude::*;

#[test]
fn ai_map_generation_placeholder_runs() {
    // Minimal Bevy app to ensure systems can be registered without panic after refactor
    let mut app = App::new();
    app.add_plugins(MinimalPlugins);
}
